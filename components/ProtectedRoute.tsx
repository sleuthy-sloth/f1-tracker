/**
 * @deprecated ProtectedRoute is no longer used.
 * Guest access is now the default — all routes work without authentication.
 * Only the fantasy page shows a sign-in prompt for unauthenticated users.
 * 
 * This file is kept as a reference for future auth-gated features.
 * See components/ProtectedRoute.backup.tsx if needed.
 * 
 * Remove date: after next major release if no auth-gated routes are added.
 */